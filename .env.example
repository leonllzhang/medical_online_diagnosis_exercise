# 数据库连接（Docker Compose 内部使用）
# 如果部署到外部数据库，替换 host 和密码
DATABASE_URL="postgresql://medlink:medlink_pass@db:5432/medlink_exam"

# JWT 密钥（生产环境请修改为随机字符串）
JWT_SECRET="change-this-to-a-random-secret-string"

# 合格分数线（百分制，默认 80）
PASS_THRESHOLD=80

# 应用配置
NEXT_PUBLIC_APP_NAME="医护互联考试考核系统"
